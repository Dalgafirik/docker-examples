# Java Runtime on Ubuntu

This example shows how to install the Java runtime manually on a plain
Ubuntu base image and run a simple Java application inside a container.

The purpose is to understand Docker image layers and runtime setup.
