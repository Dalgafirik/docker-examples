# Java Runtime (Slim Image)

This example demonstrates running a simple Java application using a
prebuilt OpenJDK slim base image.

The focus is on using an existing runtime image rather than installing
the runtime manually.
